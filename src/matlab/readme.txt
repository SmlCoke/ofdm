MATLAB 代码索引（各目录均有 README.md）：

  baseband/     任务一  单载波调制解调（yjl）
  ofdm/         任务二  OFDM，内置 fft/ifft（yjl）
  fft/          任务三  定点 FFT/IFFT 参考模型（fj）
  cordic_fft/   选做    CORDIC 与 128 点 FFT/IFFT（zcx）
  ofdm_cordic/  选做    OFDM 中 CORDIC 替换 FFT/IFFT（zcx）

选做输出：cordic_fft/figures|results（CORDIC）；ofdm_cordic/figures|results（OFDM BER）。可选汇总到 report_assets/
